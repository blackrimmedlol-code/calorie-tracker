# 减脂看板 · 数据维护交接手册

> **交接对象**：接手 `blackrimmedlol-code/calorie-tracker` **数据维护** 工作的任何 AI agent（Codex / Claude Code / 其他）。
> **原维护方**：Hermes 大总管（运行于腾讯云 VPS，2026-09 起停止服务）。本文是**规则 + 纪律的唯一汇总**。
> **配套文档**：`HERMES_DATA_GUIDE.md`（字段口径权威，写数据前必读）、`AGENTS.md`（页面设计规范与分工边界）。
> **读法**：动手前必读 §1 红线、§2 流程、§10 自检；其余按需查。

---

## 0. 一分钟总览

- **项目**：用户的 16-8 减脂卡路里观察看板，纯静态单页（GitHub Pages）。
- **线上**：https://blackrimmedlol-code.github.io/calorie-tracker/ （push 后 1-2 分钟生效）
- **你的职责**：把用户发来的**餐食 / 体重 / 活动**数据，准确写进根目录 `data.json` → 校验 → push。**页面代码（index.html）不是你的活**。
- **唯一真相**：`data.json`。任何"我记下了/我更新了记忆"都不算完成，**只有 push 成功 + 远端可见才算完成**。

---

## 1. 红线（违反即事故）

1. **只改根目录 `data.json`**。`git add` 只允许 `git add -- data.json`，**禁止 `git add -A`**，禁止 force push。
2. **不要碰 `index.html`** —— 页面/样式/图表归 Codex。你发现页面 bug 就报告，不要自己改。
3. **不要碰 `market/`** —— 美股策略台已迁到独立仓库 `blackrimmedlol-code/us-market-dashboard`，`market/` 只剩跳转归档。push 前必查：`git diff --name-only origin/main...HEAD` 若出现 `market/` 文件 → **立即停止**。
4. **不要猜体重**。没报就是没报，字段省略，不许填 0、不许自己造数。
5. **不许用 0 代替"未知"**。未知 = 省略字段或 `null`（`intake: null` 只有 records 为空时才允许）。
6. **不许把单次运动的热量加进全天消耗**。`totalCalories` 已含基础+活动消耗，禁止再叠加 `activeCalories` 或单次运动热量。
7. **不许加回"记录一餐"表单**，不许改锚点色 IKB `#002FA7`，不许引入构建工具。
8. **不要为了等一个不确定的信息卡住整条写入链路**（见 §6.3）。

---

## 2. 标准流程（每次收到数据都走这条）

```
① 确认真实日期 → ② 识别/确认数据 → ③ 读当日已有记录（合并，不覆盖）
→ ④ 写 data.json → ⑤ 跑校验 → ⑥ push（只推 data.json）→ ⑦ 回读远端验证 → ⑧ 回复用户
```

### ① 确认真实日期（必做，别省）

```bash
date '+%Y-%m-%d %H:%M %Z'
```

深夜开始的会话可能跨天续传（用户次日中午继续发消息）。**记录日期和餐次一律用当下实际时间**，不要用会话开始时间或"感觉"。历史上因为跳这一步，把 8-19 午餐错记成 8-18 深夜加餐，连带体重日期串错。

### ② 识别 / 确认数据

- 有图：先复述你看到什么（每样菜 + 份量 + 剩了多少），**再**给估算。用户曾明确问过"没告诉我识别出什么"。
- 份量识别按 `tools/README.md` 与 §5 的参照物口径做，**关键判断要放大复核一遍**。
- 用户自报了份量/做法 → 以用户自报为准，图仅参考。

### ③ 读当日已有记录（最容易出事的一步）

```bash
python3 -c "
import json
d=json.load(open('data.json'))
for x in d['days']:
    if x['date']=='2026-09-14': print(json.dumps(x,ensure_ascii=False,indent=2))
"
```

- 同一天**只能有一个** `days` 对象；新信息合并进已有日期，不许重复建。
- 新餐的 `meal` 按**实际餐次**写（同日第二餐一般是"晚餐"），别照抄上游给的"午餐"。
- **`macros` 是全天汇总，必须和已有值相加**，不是覆盖（午餐 75/110/65 + 晚餐 63/38/38 = 138/148/103）。传新值会把另一餐的宏量整个清掉。
- 追加后核对 `intake == 所有 records 之和`。

### ④ 写 data.json

饮食（脚本）：

```bash
python3 tools/update-calorie-data.py --date 2026-09-14 \
  --records '[{"name":"台式炸鸡","calories":700,"meal":"晚餐"},{"name":"卤肉饭","calories":480,"meal":"晚餐"}]' \
  --macros '{"protein":63,"carbs":38,"fat":38}' --mode append
```

- `--records` 必须是 **JSON 数组**、`--macros` 必须是 **JSON 对象**，shell 里用**单引号**包住。传纯文本会报 `JSONDecodeError: Expecting value: line 1 column 1`。
- `--mode append`（默认）按 `name` 合并：**同名会被新值覆盖**，同名不同菜请改名字。
- 该脚本**只支持 records / macros / intake / meta / --roll**，**不支持 `activity`、`body`**。

活动 / 体重（脚本不支持，python 直改）：

```bash
python3 - <<'PY'
import json, datetime
p='data.json'; d=json.load(open(p,encoding='utf-8'))
day=next((x for x in d['days'] if x['date']=='2026-09-14'), None)
if day is None:
    day={'date':'2026-09-14','intake':None,'records':[],'macros':None}
    d['days'].append(day)
day.setdefault('body',{})['weightKg']=78.85          # 合并进已有 day，别新建覆盖
d['days'].sort(key=lambda x:x['date'])
d['meta']['updatedAt']=datetime.datetime.now().astimezone().isoformat(timespec='seconds')
json.dump(d,open(p,'w',encoding='utf-8'),ensure_ascii=False,indent=2)
PY
```

字段口径细节（activity 各字段、workouts、isFinal、capturedAt、body.measuredAt/source 何时省略）**一律查 `HERMES_DATA_GUIDE.md`**，不要凭记忆。

### ⑤ 校验（不通过不许提交）

```bash
python3 tools/validate-calorie-data.py data.json     # 期望输出 VALIDATION_OK
```

### ⑥ 推送（一条命令，别手搓 git 序列）

```bash
bash tools/push-data.sh "9-14 晚餐：台式炸鸡+卤肉饭（1180大卡，全天2530）"
```

脚本内置安全序列：`pull --rebase` → 校验 → 只 `add data.json` → 有改动才 commit → push → 验证不 ahead。**看到 `PUSH_OK` + `SYNCED` 才算成功**。

### ⑦ 回读远端验证（优先走 git 通道）

```bash
git fetch origin main --quiet && git show origin/main:data.json | python3 -c "
import json,sys; d=json.load(sys.stdin)
print([x['date'] for x in d['days']][-3:], d['meta']['updatedAt'])"
```

`git show origin/main:data.json` 读的是远端内容，等价于线上 raw 且不受 CDN 抖动影响。raw.githubusercontent.com 的 curl 在国内常超时，**不要**因为一次 curl 失败就重写数据重推。

### ⑧ 回复用户

每餐回复固定包含：**每样菜热量（表格）→ 总热量 + 蛋白/碳水/脂肪 → 今日对账（当天全部餐次合计）→ 超目标标 ⚠️ → 咸口/断食窗口提醒**。

---

## 3. 活动 / 体重数据（高频、易漏）

⚠️ **收到活动或体重截图，分析完必须当场写入并 push**。历史上漏写被用户追问"push 到看板了没有"。**"我分析完了"≠"看板已更新"**。

- **整体活动截图** → 写 `activity`：`activeCalories / totalCalories / exerciseMinutes / standHours / steps / distanceKm / 目标值 / capturedAt / isFinal`。
  - 苹果摘要页红字**"共 XXXX 大卡"= 全天总消耗**（含静息）→ `totalCalories`，**不是活动环值**（比环值大 3-5 倍）。
  - 摘要页"锻炼"图下方**"总计 X 小时 Y 分钟"不是当天锻炼分钟数**（那是周合计，实见过 15h31m）→ **绝不写进 `exerciseMinutes`**；那个字段只取锻炼环的 `xx/30 分钟`。
  - 摘要页**不列单次训练**：只有摘要页时 `workouts: []`，并主动说"这天若有专项运动，把体能训练页发我补明细"，**不要凭锻炼分钟数臆造 workout**。
- **单次运动截图** → 只写 `activity.workouts`（含 `source`）。**绝不能反推全天步数/总消耗**。后续拿到同日整体截图时合并整体字段并**保留已有 workouts**；按 `日期+开始时间+类型` 查重。
- **体重** → `body: {"weightKg": 78.85}`，单位 kg。
  - 该日期已有条目（有餐/活动）→ 合并进现有 day。
  - 只有体重：新建 `{"date":..., "intake":null, "records":[], "macros":null, "body":{...}}`。
  - 未提供称重时间/来源 → **省略** `measuredAt`/`source`，不许猜。
  - 同一天多次称重：用户指定优先，未指定取晨起空腹值。
- **关键字段（站立环 / 共 X 大卡 / 步数 / 距离）必须分区放大复核一遍**。整图一次性读数会把图表小字串进去（实测同一张图整图读"站立 12/12"，放大读出"7/12"，再放大才确认真值）。**两次冲突以放大复核为准，不要取平均**。
- 状态栏 12 小时制**不显示 AM/PM**：读数与热量量级矛盾时，用 `总消耗 ≈ 静息速率 × 已过小时 + 活动消耗` 反推真实时刻（实测"9:57"实为 21:57），`capturedAt` 写反推时间。
- 断言：`activeCalories < totalCalories`；日期唯一；未知值不填 0；`meta.updatedAt` 必更。

---

## 4. 体重曲线 / 均线 / 插值（用户会追问页面表现）

- 页面 x 轴是**近 30 天每天一槽**，体重原始序列是**散点（scatter）**：只画有值的点，**不连线、缺日不插值、新数字只落在它自己那天**。
- 平滑只由 **7 日均线**承担，算法是"**同一 7 天窗口内已记录点取平均**"（缺日跳过，不是"7 天和 ÷7"），窗口内有效点 <3 则断开。
  - 后果：缺记录期间均线**不空**，而是原地踏步；补上实测点会出现**台阶**（9-10~9-13 无体重 → 均线 77.8/77.8/77.7/77.7；9-14 补 78.85 后跳到 ≈78.0）。
- 摘要三格：**最新体重 / 较上次（对比上一次有记录的点，不是昨天）/ 7 日均线**；样本 <3 时均线显示 `—`。单点记录只显示"最新体重"，**要如实告诉用户还需 ≥2 次才成趋势**。
- **线性插值补记只在用户明确说"补上"时做**：取两端实测点 A、B，第 k 天 = `A + (B-A)*k/n`，保留 2 位小数；合并进已有 day（`setdefault('body',{})`），不新建；必须**口头 + commit message 标注"线性插值补记"**。
- **插值只让曲线连贯，不是实测**。判读趋势只看实测点 —— 看板上 9-4~9-7、9-10~9-13 是插值段，不要当成真实日增速（用户已知悉，别再把插值段当实测讲）。
- 断食期体重短期持平/上浮是水分糖原波动（咸口菜可致 +0.5kg 储水）；只看 7 日均线，单日波动不调整方案。

---

## 5. 份量估算口径（最容易低估的地方）

**铁律**：

1. **餐前餐后双图都分析**：餐前定原始量，餐后定剩余比例；实际摄入 = 原始量 × 吃掉比例。
2. **每项按参照物推算**，不许凭感觉。
3. **每项给区间（低-高）+ 中值**，总热量报 `≈ 中值（低-高）`。
4. 一次读数不笃定 → **region 放大复核**；两次分歧大 → **取保守中间值并说明口径**。同一张图两次读数可以差一倍（实测牛排 150-200g vs 250-300g，初估 555 被另一 AI 估 950，复核后 720）。
5. 多 AI 交叉验证：**先比区间再比中值**；中值差 <10% 属噪声；最大假象来源是"对方单列油 vs 自己把油折进单位热量"，先拆口径再比；拆解后仍差 >15% 才回图上复核那一项。

**参照物**：外卖牛排托盘 ≈24×16cm；标准餐盒 ≈17×12cm；平碗口径 11-12cm，1 平碗熟饭 ≈150g；鸡蛋直径 ≈5cm；成年男拳 ≈200ml（1 拳熟饭/熟肉 ≈150g，1 拳蔬菜 ≈200g）；手掌（不含指）≈100g 瘦肉；拇指 ≈15g 油脂；扑克牌 ≈85-100g 熟肉。

**摊开 ≠ 量大**：用"覆盖面积 × 平均厚度 × 密度"算体积（熟米饭密度 0.8-0.9 g/cm³）。例：米饭铺 326cm² × 厚 0.7cm ≈ 228cm³ ≈ 200g 熟饭（≈1.5 拳）。

**常查单位热量**：瘦牛排（煎）220-260 kcal/100g 熟（含煎油 2-4g/100g）；肥牛排 300-350；**煎牛排额外用油每片 3-5g ≈ 27-45 kcal，别漏算**；熟白米饭 116-130/100g（1 平碗 ≈175-195）；溏心蛋 70-80/颗；外卖整盒饭 ≈350-390（≈300g）；照烧/黑椒酱 30-60/份。

**常见数字速查**（这些是踩过坑校准的，别用美版数字）：

- 麦当劳奥利奥麦旋风（中国区标准杯）≈ **266 大卡**（美版 510，勿用）
- 双层鱼柳包（双层深海鳕鱼堡 209g）**485**（P28/C46/F21）
- 大玉米杯 87 / 小玉米杯 53；含糖柠檬红茶中杯 ≈150-190
- DQ 奥利奥暴风雪：100g ≈230 大卡 → 标准杯 450-550，小/迷你杯 250-350（同样勿用美版 380）

---

## 6. 行为纪律（这一节决定用户会不会信任你）

### 6.1 收到数据就记，不要问"要不要记"

用户 9-14 明确："**反正我发给你，你就帮我记录吧**"。收到体重/饮食/活动 → **直接按流程入账并 push**，不要先问"要不要记""记哪天"。

### 6.2 不要用多选表单追问细节

份量/时段不全时，**按最可能的默认口径直接入账**，在回复里标注口径并说"不对我再改"。**严禁弹多选表单让人选**（用户曾质问："这个指令不是发过给你了吗？为什么还不跟着做"）。只有真正影响记账结论的缺失项（例如缺日要不要插值）才值得开口。

### 6.3 不确定做法时：中值入账 + 当场问一句

例：用户只报"3 个鸡蛋"没说水煮/油煎（水煮 502 / 煎 620，差 ±60）→ **按中间值入账**（`name` 里注明"做法未确认，按水煮/煎中值"），回复末尾问一句，用户答了再改。**不要为了等答案卡住写入链路**。

### 6.4 长链条要有进度反馈

多步操作（识别 → 估算 → 写入 → push）中途发**短进度消息**，别整段沉默几分钟后才给最终结论。进度说明**单独一条发**，别和长工具调用挤在同一条里。

### 6.5 图上没报的菜不算

用户只报自己吃的。图里可见但没提到的菜（卤鸡爪、腊肠、肉皮等）**不计数、不追问** —— 用户说过"没吃的就不报了"。

### 6.6 超目标不慌，也不节食补偿

超 1600-1800 照实报，提醒"偶尔一天没关系，明天回区间，别节食补偿"。咸口菜（梅菜/卤味/蘸酱）主动提醒多喝水，短期 +0.5kg 是储水。

### 6.7 深夜"该不该吃 X"的咨询

结构固定：**结论先行 → 热量账（吃得起不是否决点）→ 真否决点（蛋白缺口 / 已过进食窗口 / 睡眠与水秤）→ 折中方案（把冲动存到次日进食窗口内的"计划性犒赏"）→ 行为刹车清单（热水+刷牙 → 15 分钟延迟 → 转移注意 → 确认是饿不是馋 → 给冲动确定出口）**。

- 给**明确倾向**，别骑墙；用户习惯先问一句再做决定，明确结论 + 可执行替代最有效（实测理性方案下他会自控）。
- **没吃就不写 data.json、不 push**；等真吃了发图再记账。

### 6.8 缺账盘点用脚本，别手工翻 JSON

```bash
python3 tools/audit-missing-days.py data.json 10
```

回复用"日期 × 缺什么"的表，并标出该日**已有**的部分（"9-10 活动已记 477 大卡/9242 步，缺三餐"），再附体重锚点（"体重最后一次是 x-xx 的 xx.x kg"）。**只报缺失，不假设原因**：只记一餐的那天要问清"没吃"还是"漏记"。

---

## 7. 推送 / 网络（Git）纪律

**错误分类是硬性约束，别混为一谈：**

| 症状 | 归类 | 允许的动作 |
|---|---|---|
| `rejected` / `non-fast-forward` / SHA 分叉 | **Git 同步错误**（远端有新提交） | fetch+rebase **一次** → 重新校验 JSON → 重推 **一次**。再失败**立即停**，人工处理 |
| `curl 28` / 连接超时 / 不可达 / `Operation too slow. Less than 1000 bytes/sec` | **网络错误** | 换 IP/重试，**有上限**（默认 3 次）。**禁止 60 轮长循环** |
| `Please commit or stash them` | 工作区脏 | 见下 |

- 推送前**必须**检查：`git diff --name-only origin/main...HEAD` 无 `market/` 文件。
- 工作区脏的原因通常是"先写后推"：`push-data.sh` 会自动处理（只有 data.json 改动 → 校验后直接提交；含其他文件 → 自动 stash → pull → pop，其他文件保持未提交）。
- 看到 `STILL_AHEAD` / `GIT_DESYNC` / `REBASE_CONFLICT` / `ABORT` → **停下排查**，不许继续重试。
- rebase 冲突：立即 `git rebase --abort`，保留本地数据，人工处理，**绝不强制覆盖**。
- **本地领先 origin 是正常的**（push 中途失败遗留）；`pull --rebase` 会自动承接远端提交，基本零冲突（页面归 Codex、数据归你，两边不重叠）。
- 详细网络加固（国内出口对 github.com 间歇性限流、IP 钉法）见 `tools/git-push-retry.sh` 头部注释；**如果新环境网络正常，用 `tools/push-data.sh` 即可，不需要那套 IP 逻辑**。

---

## 8. 定时任务（现状）

- **每日 0:00 滚动 trend 的 cron 已取消（2026-08-31）**：`trend` 字段页面 JS 已零引用，纯浪费。
- ⚠️ 因此 `AGENTS.md` 里"每日 0:00 有 cron 自动滚动"那段**已过期**，别按它安排 pull 时机（pull 永远是好习惯，但不是因为这个 cron）。
- 接手后若想恢复自动滚动：用 `python3 tools/update-calorie-data.py --roll`（**`--roll` 模式不需要 `--date`**），脚本自身"无改动则静默退出"。

---

## 9. 迁移注意（VPS 停机后哪些能力会消失）

原管线依赖 Hermes VPS 上的三样东西，接手方要自己补上：

1. **看图能力**：原流程是"网关自动注入图片描述 → 必要时放大复核"。新 agent **必须自己有视觉能力**；若没有，就让用户用参照物口述份量（拳/碗/包装规格），**不要凭空猜**。
2. **专职养生 agent**：原来会把餐食转发给 wellness 专职 agent 做独立估算、交叉验证。现在由你自己估，**多 AI 交叉验证那一套（§5 第 5 条）仍然适用** —— 与用户手上的另一个 AI 对一次账是很好的校准。
3. **脚本位置**：原来在 `~/.hermes/scripts/`。现在**已随仓库带过来**，放在 `tools/`（见 `tools/README.md`，路径均已做成可移植）。

---

## 10. 自检清单（每次 push 前逐条过）

- [ ] `date` 确认过真实日期，`--date` 与 `meal` 用的是当下实际时间
- [ ] 同一天只有一个 `days` 对象；新数据是**合并**进已有 day
- [ ] `macros` 是**全天累加**后的值，不是覆盖
- [ ] `intake` == 所有 `records[].calories` 之和
- [ ] 未知值用省略/`null`，**没有写 0**
- [ ] 没有重复食物（同名+同餐次+同热量）；workouts 无重复
- [ ] 单次运动热量没有混进 `totalCalories`
- [ ] `meta.updatedAt` 已更新（含 `+08:00`）
- [ ] `python3 tools/validate-calorie-data.py` → `VALIDATION_OK`
- [ ] `git diff --name-only origin/main...HEAD` 无 `market/` 文件
- [ ] push 输出 `PUSH_OK` + `SYNCED`
- [ ] 已回读 `origin/main:data.json` 确认当天记录在远端

---

## 11. 参考文件

| 文件 | 内容 |
|---|---|
| `HERMES_DATA_GUIDE.md` | **字段口径权威**：每天结构、饮食、Apple Fitness、单次运动、体重、缺失数据语义、提交前检查 |
| `AGENTS.md` | 页面设计规范（瑞士风、IKB 蓝、禁表单）、分工边界、硬性约束 |
| `tools/README.md` | 脚本清单 + 可移植路径说明 + 在新环境怎么跑 |
| `tools/update-calorie-data.py` | 写 records/macros/intake/meta，`--roll` 滚趋势 |
| `tools/validate-calorie-data.py` | 提交前硬约束校验（唯一日期、禁 0、updatedAt 必更） |
| `tools/push-data.sh` | 安全推送（pull→校验→只 add data.json→commit→push→验同步） |
| `tools/git-push-retry.sh` | 国内出口限流环境下的强化版推送（IP 钉 + 错误分类 + 重试上限） |
| `tools/audit-missing-days.py` | 缺账盘点 |
| `index.html` | 页面（**Codex 维护，别动**） |

---

## 12. 一句话版本

> **数据只进 `data.json`；收到就记、记完就推、推完回读验证；不确定就按中值入账并标注口径，绝不追问式空转；页面代码不碰，`market/` 不碰，`git add -A` 不碰。**
